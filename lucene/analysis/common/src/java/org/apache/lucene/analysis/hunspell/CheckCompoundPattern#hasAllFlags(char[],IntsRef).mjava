  private boolean hasAllFlags(char[] flags, IntsRef forms) {
    for (char flag : flags) {
      if (!dictionary.hasFlag(forms, flag, scratch)) {
        return false;
      }
    }
    return true;
  }

