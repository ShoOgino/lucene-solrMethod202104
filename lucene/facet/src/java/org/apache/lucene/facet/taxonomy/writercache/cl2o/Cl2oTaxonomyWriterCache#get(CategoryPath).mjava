  @Override
  public int get(CategoryPath categoryPath) {
    lock.readLock().lock();
    try {
      return cache.getOrdinal(categoryPath);
    } finally {
      lock.readLock().unlock();
    }
  }

