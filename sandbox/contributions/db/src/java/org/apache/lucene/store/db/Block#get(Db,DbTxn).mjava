    protected void get(Db blocks, DbTxn txn)
        throws IOException
    {
        try {
            blocks.get(txn, key, data, 0);
        } catch (DbException e) {
            throw new IOException(e.getMessage());
        }
    }

