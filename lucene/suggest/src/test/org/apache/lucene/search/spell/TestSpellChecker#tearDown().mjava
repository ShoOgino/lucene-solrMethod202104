  @Override
  public void tearDown() throws Exception {
    userindex.close();
    if (!spellChecker.isClosed()) {
      spellChecker.close();
    }
    spellindex.close();
    analyzer.close();
    super.tearDown();
  }

