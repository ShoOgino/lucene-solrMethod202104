  public void set(Term term) {
    if (term == null) {
      reset();
      return;
    }
    bytes.copy(term.bytes());
    field = term.field().intern();
    currentFieldNumber = -1;
    this.term = term;
  }

