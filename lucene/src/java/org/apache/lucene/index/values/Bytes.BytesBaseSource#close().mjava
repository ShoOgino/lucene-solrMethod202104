    public void close() throws IOException {
      data.close();
      try {
        if (datIn != null)
          datIn.close();
      } finally {
        if (idxIn != null) // if straight
          idxIn.close();
      }
    }

