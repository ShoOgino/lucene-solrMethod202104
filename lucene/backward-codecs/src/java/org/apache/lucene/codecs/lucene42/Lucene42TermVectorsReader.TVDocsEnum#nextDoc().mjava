    @Override
    public int nextDoc() throws IOException {
      if (doc == -1 && (liveDocs == null || liveDocs.get(0))) {
        return (doc = 0);
      } else {
        return (doc = NO_MORE_DOCS);
      }
    }

