  @Override
  public synchronized void decref() throws IOException {
    refCnt--;
    if (refCnt == 0) {
      if (indexWriter != null) {
        indexWriter.close();
      }
      directoryFactory.close();
    }
  }

