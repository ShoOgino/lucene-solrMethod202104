  /** returns x == 0 ? 0 : Math.floor(Math.log(x) / Math.log(base)) */
  static int log(int x, int base) {
    int ret = 0;
    while (x >= base) {
      x /= base;
      ret++;
    }
    return ret;
  }

