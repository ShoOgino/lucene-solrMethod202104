    @Override
    public void finish(int docCount) throws IOException {
      try {
          datOut.close();
      } finally {
        try {
          if (idxOut != null)
            idxOut.close();
        } finally {
          if (pool != null)
            pool.reset();
        }
      }
    }

