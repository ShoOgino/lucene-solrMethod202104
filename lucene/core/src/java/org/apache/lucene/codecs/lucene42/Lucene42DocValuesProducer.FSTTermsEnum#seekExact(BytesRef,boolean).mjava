    @Override
    public boolean seekExact(BytesRef text, boolean useCache) throws IOException {
      if (in.seekExact(text) == null) {
        return false;
      } else {
        return true;
      }
    }

