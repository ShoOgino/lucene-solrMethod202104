  @Override
  public void close() throws IOException {
    super.close();

    // release resources and ease GC
    if (ae != null) {
      ae.destroy();
      ae = null;
    }
    if (cas != null) {
      cas.release();
      cas = null;
    }
  }

