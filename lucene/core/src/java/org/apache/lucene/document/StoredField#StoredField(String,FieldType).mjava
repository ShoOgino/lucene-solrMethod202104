  /**
   * Expert: allows you to customize the {@link FieldType}.
   *
   * @param name field name
   * @param type custom {@link FieldType} for this field
   * @throws IllegalArgumentException if the field name or type is null.
   */
  protected StoredField(String name, FieldType type) {
    super(name, type);
  }

