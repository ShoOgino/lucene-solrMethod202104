    @Override
    public boolean isIndexTerm(BytesRef term, int docFreq) {
      if (docFreq >= docFreqThresh || count >= interval) {
        count = 0;
        return true;
      } else {
        count++;
        return false;
      }
    }

