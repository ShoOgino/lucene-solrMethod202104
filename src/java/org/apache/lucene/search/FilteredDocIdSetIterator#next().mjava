  // @Override
  public final boolean next() throws IOException{
    while (_innerIter.next()) {
      int doc = _innerIter.doc();
      if (match(doc)) {
        _currentDoc = doc;
        return true;
      }
    }
    return false;
  }

