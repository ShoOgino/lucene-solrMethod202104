    @Override
    public void close() throws IOException {
      super.close();
      if (datIn != null) {
        datIn.close();
      }
      if (idxIn != null) {
        idxIn.close();
      }
    }

