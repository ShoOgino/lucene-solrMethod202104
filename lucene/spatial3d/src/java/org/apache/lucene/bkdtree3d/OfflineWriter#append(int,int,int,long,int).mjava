  @Override
  public void append(int x, int y, int z, long ord, int docID) throws IOException {
    out.writeInt(x);
    out.writeInt(y);
    out.writeInt(z);
    out.writeLong(ord);
    out.writeInt(docID);
    countWritten++;
  }

