    @Override
    public Number next() {
      if (!hasNext()) {
        throw new NoSuchElementException();
      }
      long value;
      if (upto < size) {
        value = iter.next();
      } else {
        value = 0;
      }
      upto++;
      // TODO: make reusable Number
      return value;
    }

