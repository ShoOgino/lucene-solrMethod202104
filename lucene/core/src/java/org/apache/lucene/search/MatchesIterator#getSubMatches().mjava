  /**
   * Returns a MatchesIterator that iterates over the positions and offsets of individual terms
   * within the current match
   *
   * <p>Returns {@code null} if there are no submatches (ie the current iterator is at the leaf
   * level)
   *
   * <p>Should only be called after {@link #next()} has returned {@code true}
   */
  MatchesIterator getSubMatches() throws IOException;

