    @Override
    public boolean isWithin(double x, double y, double z)
    {
        for (SidedPlane edge : edges) {
            if (!edge.isWithin(x,y,z))
                return false;
        }
        return true;
    }

