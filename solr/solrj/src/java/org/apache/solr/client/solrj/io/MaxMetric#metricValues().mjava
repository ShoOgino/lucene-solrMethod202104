  public Map<String, Double> metricValues() {
    Map m = new HashMap();
    if(isDouble) {
      m.put(MAX,doubleMax);
    } else {
      doubleMax = (double)longMax;
      m.put(MAX,doubleMax);
    }
    return m;
  }

