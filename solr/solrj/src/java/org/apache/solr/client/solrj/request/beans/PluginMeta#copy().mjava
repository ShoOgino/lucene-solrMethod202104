  public PluginMeta copy() {
    PluginMeta result = new PluginMeta();
    result.name = name;
    result.klass = klass;
    result.version = version;
    result.config = config;
    return result;
  }

