  /** How many transitions this state has. */
  public int getNumTransitions(int state) {
    int count = states[2*state+1];
    if (count == -1) {
      return 0;
    } else {
      return count;
    }
  }

