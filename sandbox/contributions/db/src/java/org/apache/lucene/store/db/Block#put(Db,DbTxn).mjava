    protected void put(Db blocks, DbTxn txn)
        throws IOException
    {
        try {
            blocks.put(txn, key, data, 0);
        } catch (DbException e) {
            throw new IOException(e.getMessage());
        }
    }

