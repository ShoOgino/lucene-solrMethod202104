    @Override
    public synchronized void finish(int docCount) throws IOException {
      if (datOut != null)
        datOut.close();
      if (idxOut != null)
        idxOut.close();
      if (pool != null)
        pool.reset();
    }

