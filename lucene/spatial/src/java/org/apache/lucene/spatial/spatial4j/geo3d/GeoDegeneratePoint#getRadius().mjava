    /** Returns the radius of a circle into which the GeoSizeable area can
     * be inscribed.
     *@return the radius.
     */
    @Override
    public double getRadius() {
        return 0.0;
    }

