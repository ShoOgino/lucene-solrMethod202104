  /**
   * Stops all cores.
   */
  public void shutdown() {
    synchronized(cores) {
      for(CoreDescriptor descriptor : cores.values()) {
        SolrCore core = descriptor.getCore();
        if( core != null ) {
          core.close();
        }
      }
      cores.clear();
    }
  }

