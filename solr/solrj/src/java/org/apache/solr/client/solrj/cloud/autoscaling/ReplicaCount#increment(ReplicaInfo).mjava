  void increment(ReplicaInfo info) {
    switch (info.getType()) {
      case NRT:
        nrt++;
        break;
      case PULL:
        pull++;
        break;
      case TLOG:
        tlog++;
        break;
      default:
        nrt++;
    }
  }

