  @Override
  public void close() throws SQLException {
    if(closed) {
      return;
    }
    try {
      this.solrClientCache.close();
      this.closed = true;
    } catch (Exception e) {
      throw new SQLException(e);
    }
  }

