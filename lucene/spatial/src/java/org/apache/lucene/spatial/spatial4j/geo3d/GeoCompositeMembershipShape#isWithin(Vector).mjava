    @Override
    public boolean isWithin(Vector point)
    {
        for (GeoMembershipShape shape : shapes) {
            if (shape.isWithin(point))
                return true;
        }
        return false;
    }

