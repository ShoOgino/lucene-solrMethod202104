  public void destroy() {
    if (multicore != null) {
    multicore.shutdown();
      multicore = null;
    }
    if( singlecore != null ) {
      singlecore.close();
      singlecore = null;
    }
  }

