  // nocommit nuke
  public static boolean isBytes(Type type) {
    switch(type) {
    case BYTES_VAR_STRAIGHT:
    case BYTES_FIXED_STRAIGHT:
      return true;
    default:
      return false;
    }
  }

