    public Option[] getOptions() {
      return new Option[] {
          Option.builder("get")
              .argName("URL")
              .hasArg()
              .required(false)
              .desc("Send a GET request to a Solr API endpoint")
              .build()
      };
    }

