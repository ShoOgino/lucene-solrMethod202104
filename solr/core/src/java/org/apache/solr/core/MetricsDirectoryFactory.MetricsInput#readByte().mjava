    @Override
    public byte readByte() throws IOException {
      totalMeter.mark();
      if (withDetails) {
        totalHistogram.update(1);
        meter.mark();
        histogram.update(1);
      }
      return in.readByte();
    }

