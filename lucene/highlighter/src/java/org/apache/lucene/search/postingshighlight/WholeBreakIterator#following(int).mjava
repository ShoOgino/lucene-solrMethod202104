  @Override
  public int following(int pos) {
    if (pos < 0 || pos > len) {
      throw new IllegalArgumentException("offset out of bounds");
    } else if (pos == len) {
      return DONE;
    } else {
      return last();
    }
  }

