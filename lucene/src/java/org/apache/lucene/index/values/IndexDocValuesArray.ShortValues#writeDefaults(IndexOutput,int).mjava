    @Override
    protected void writeDefaults(IndexOutput out, int num) throws IOException {
      final short zero = 0;
      for (int i = 0; i < num; i++) {
        out.writeShort(zero);
      }
    }

