    Suggester init(String coll, String shard, Policy.Session session) {
      this.coll = coll;
      this.shard = shard;
      this.session = session.copy();
      this.operation = init();
      return this;
    }

