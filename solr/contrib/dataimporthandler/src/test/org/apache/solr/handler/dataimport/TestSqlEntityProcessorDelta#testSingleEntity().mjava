  @Test
  public void testSingleEntity() throws Exception {
    assumeIncomaptibleLocale();
    singleEntity(1);
    changeStuff();
    int c = calculateDatabaseCalls();
    singleEntity(c);
    validateChanges();
  }

