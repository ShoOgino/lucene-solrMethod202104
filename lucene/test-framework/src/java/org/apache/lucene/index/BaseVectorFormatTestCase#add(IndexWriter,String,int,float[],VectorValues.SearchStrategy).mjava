  private void add(
      IndexWriter iw,
      String field,
      int id,
      float[] vector,
      VectorValues.SearchStrategy searchStrategy)
      throws IOException {
    add(iw, field, id, random().nextInt(100), vector, searchStrategy);
  }

