    @SuppressWarnings({"unchecked"})
    public Object next() {
      double[] row = d[index++];
      List list = new ArrayList();
      for(double value : row) {
        list.add(value);
      }

      return list;
    }

