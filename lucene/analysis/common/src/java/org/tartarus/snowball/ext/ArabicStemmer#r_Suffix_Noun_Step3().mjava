private boolean r_Suffix_Noun_Step3() {
  ket = cursor;
  if (find_among_b(a_16) == 0)
  {
    return false;
  }
  bra = cursor;
  if (!(limit >= 3))
  {
    return false;
  }
  slice_del();
  return true;
}

