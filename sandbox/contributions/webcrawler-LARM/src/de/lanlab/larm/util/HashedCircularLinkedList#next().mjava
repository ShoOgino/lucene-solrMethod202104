    public Object next()
    {
        current = nextEntry(current);
        if(current != null)
        {
            return current.element;
        }
        else
        {
            return null;
        }
    }

