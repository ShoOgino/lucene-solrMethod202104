    @Override
    public boolean isWithin(Vector point)
    {
        for (SidedPlane edge : edges) {
            if (!edge.isWithin(point))
                return false;
        }
        return true;
    }

