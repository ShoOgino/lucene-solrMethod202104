  /**
   * Remove all fields and boosts from the document
   */
  @Override
  public void clear()
  {
    super.clear();
    if( _boost != null ) {
      _boost.clear();
    }
    if(_keepDuplicates != null ) {
      _keepDuplicates.clear();
    }
  }

