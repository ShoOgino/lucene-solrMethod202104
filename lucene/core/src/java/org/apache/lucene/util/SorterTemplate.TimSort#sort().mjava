    void sort() {
      do {
        ensureInvariants();

        // Push a new run onto the stack
        pushRunLen(nextRun());

      } while (runEnd(0) <= hi);

      exhaustStack();
      assert runEnd(0) == hi + 1;
    }

