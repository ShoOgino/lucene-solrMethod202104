  @Override
  public String toString() {
    final String name = getClass().getSimpleName();
    try {
      return name + "(term:" + getTerm().utf8ToString() +")";
    } catch (Exception e) {
      return name;
    }
  }

