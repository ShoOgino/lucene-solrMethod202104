  /**
   * Return the maximal recall for the input quality query. It is the number of relevant docs this
   * Judge "knows" for the query.
   *
   * @param query the query whose maximal recall is needed.
   */
  public int maxRecall(QualityQuery query);

