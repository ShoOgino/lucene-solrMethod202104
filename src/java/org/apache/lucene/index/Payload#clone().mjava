    /**
     * Clones this payload by creating a copy of the underlying
     * byte array.
     */
    public Object clone() {
      Payload clone = new Payload(this.toByteArray());
      return clone;
    }

