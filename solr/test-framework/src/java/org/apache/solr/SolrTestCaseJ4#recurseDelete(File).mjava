  /**
   * @see IOUtils#rm(Path...)
   */
  @Deprecated()
  public static boolean recurseDelete(File f) {
    try {
      IOUtils.rm(f.toPath());
      return true;
    } catch (IOException e) {
      System.err.println(e.toString());
      return false;
    }
  }

