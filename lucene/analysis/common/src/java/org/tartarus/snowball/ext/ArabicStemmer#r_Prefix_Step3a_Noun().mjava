private boolean r_Prefix_Step3a_Noun() {
  int among_var;
  bra = cursor;
  among_var = find_among(a_6);
  if (among_var == 0)
  {
    return false;
  }
  ket = cursor;
  switch (among_var) {
    case 1:
      if (!(limit > 5))
      {
        return false;
      }
      slice_del();
      break;
    case 2:
      if (!(limit > 4))
      {
        return false;
      }
      slice_del();
      break;
  }
  return true;
}

