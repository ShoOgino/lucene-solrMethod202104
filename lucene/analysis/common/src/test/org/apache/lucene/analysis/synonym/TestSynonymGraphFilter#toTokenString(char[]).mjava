  private static String toTokenString(char[] chars) {
    StringBuilder b = new StringBuilder();
    for (char c : chars) {
      if (b.length() > 0) {
        b.append(' ');
      }
      b.append(c);
    }
    return b.toString();
  }

