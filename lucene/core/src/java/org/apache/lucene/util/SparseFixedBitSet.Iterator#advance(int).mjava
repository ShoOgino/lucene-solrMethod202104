    @Override
    public int advance(int target) throws IOException {
      if (target >= length) {
        return doc = NO_MORE_DOCS;
      } else {
        doc = target;
      }

      return currentOrNextDoc();
    }

