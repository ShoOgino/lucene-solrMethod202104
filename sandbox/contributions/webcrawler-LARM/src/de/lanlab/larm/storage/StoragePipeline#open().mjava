    /**
     * open all docStorages
     */
    public void open()
    {
        for (Iterator it = docStorages.iterator(); it.hasNext(); )
        {
            ((DocumentStorage) it.next()).open();
        }
        isOpen = true;
    }

