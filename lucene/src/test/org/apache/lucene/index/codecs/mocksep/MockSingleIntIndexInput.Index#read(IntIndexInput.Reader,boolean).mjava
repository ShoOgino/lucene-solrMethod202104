    @Override
    public void read(IntIndexInput.Reader indexIn, boolean absolute)
      throws IOException {
      if (absolute) {
        fp = indexIn.readVLong();
      } else {
        fp += indexIn.readVLong();
      }
    }

