    @Override
    public int size() {
      // TODO: add faster implementation!
      int c = 0;
      final Iterator<String> it = iterator();
      while (it.hasNext()) {
        it.next();
        c++;
      }
      return c;
    }

