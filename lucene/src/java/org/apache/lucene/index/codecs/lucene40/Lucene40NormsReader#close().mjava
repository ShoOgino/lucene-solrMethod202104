  @Override
  public void close() throws IOException {
    try {
      if (openFiles != null) {
        IOUtils.close(openFiles);
      }
    } finally {
      norms = null;
      openFiles = null;
    }
  }

