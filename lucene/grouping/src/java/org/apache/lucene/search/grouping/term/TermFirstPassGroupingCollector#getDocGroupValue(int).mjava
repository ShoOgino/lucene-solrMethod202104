  @Override
  protected BytesRef getDocGroupValue(int doc) throws IOException {
    if (doc > index.docID()) {
      index.advance(doc);
    }
    if (doc == index.docID()) {
      return index.binaryValue();
    } else {
      return null;
    }
  }

