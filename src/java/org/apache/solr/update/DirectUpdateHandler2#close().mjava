  public void close() throws IOException {
    log.info("closing " + this);
    synchronized(this) {
      doDeletions();
      closeSearcher();
      closeWriter();
    }
    log.info("closed " + this);
  }

