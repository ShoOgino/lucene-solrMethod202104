  public void addDoc(String doc)
  {
    if (doc == null) {
      // should throw runtime exception
      return;
    }
    try {
      URL docURL = new URL(doc);
      addDoc(docURL);
    } catch (Exception e) {
      // ignore for now
    }
  }

