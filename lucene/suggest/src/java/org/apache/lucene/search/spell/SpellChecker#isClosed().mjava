  /**
   * Returns <code>true</code> if and only if the {@link SpellChecker} is closed, otherwise <code>
   * false</code>.
   *
   * @return <code>true</code> if and only if the {@link SpellChecker} is closed, otherwise <code>
   *     false</code>.
   */
  boolean isClosed() {
    return closed;
  }

