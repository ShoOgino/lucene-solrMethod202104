  /**
   * Convert an aggregation form distance value back to an actual distance. This is meant to be used
   * in conjunction with toAggregationForm() and aggregateDistances(). Distances should be converted
   * to aggregation form before aggregation is attempted, and they should be converted back from
   * aggregation form to yield a final result.
   *
   * @param aggregateDistance is the aggregate form of the distance.
   * @return the combined distance.
   */
  public default double fromAggregationForm(final double aggregateDistance) {
    return aggregateDistance;
  }

