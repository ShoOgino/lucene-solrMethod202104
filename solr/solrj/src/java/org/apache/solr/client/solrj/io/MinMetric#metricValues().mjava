  public Map<String, Double> metricValues() {
    Map m = new HashMap();
    if(isDouble) {
      m.put(MIN,doubleMin);
    } else {
      doubleMin = (double)longMin;
      m.put(MIN,doubleMin);
    }
    return m;
  }

