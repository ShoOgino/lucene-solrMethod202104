  /**
   * Validate that queries and this Judge match each other. To be perfectly valid, this Judge must
   * have some data for each and every input quality query, and must not have any data on any other
   * quality query. <b>Note</b>: the quality benchmark run would not fail in case of imperfect
   * validity, just a warning message would be logged.
   *
   * @param qq quality queries to be validated.
   * @param logger if not null, validation issues are logged.
   * @return true if perfectly valid, false if not.
   */
  public boolean validateData(QualityQuery qq[], PrintWriter logger);

