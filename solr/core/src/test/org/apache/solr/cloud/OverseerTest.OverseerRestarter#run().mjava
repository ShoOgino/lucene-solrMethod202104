    @Override
    public void run() {
      try {
        overseerClient = electNewOverseer(zkAddress);
      } catch (Throwable t) {
        //t.printStackTrace();
      }
      while (run) {
        if(random.nextInt(20)==1){
          try {
            overseerClient.close();
            overseerClient = electNewOverseer(zkAddress);
          } catch (Throwable e) {
            //e.printStackTrace();
          }
        }
        try {
          Thread.sleep(100);
        } catch (Throwable e) {
          //e.printStackTrace();
        }
      }
      try {
        overseerClient.close();
      } catch (Throwable e) {
        //e.printStackTrace();
      }
    }

