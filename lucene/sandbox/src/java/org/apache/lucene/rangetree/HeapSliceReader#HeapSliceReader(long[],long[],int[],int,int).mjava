  HeapSliceReader(long[] values, long[] ords, int[] docIDs, int start, int end) {
    this.values = values;
    this.ords = ords;
    this.docIDs = docIDs;
    curRead = start-1;
    this.end = end;
  }

