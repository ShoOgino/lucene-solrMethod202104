    /**
     *  The teardown method for JUnit
     * TODO: remove indexDir?
     */
    @Override
    protected void tearDown() throws Exception {
        searcher.close();
        dir.close();
        super.tearDown();
    }

