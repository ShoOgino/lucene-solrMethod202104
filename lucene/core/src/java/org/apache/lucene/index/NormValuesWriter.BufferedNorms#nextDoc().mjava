    @Override
    public int nextDoc() {
      docID++;
      if (docID == maxDoc) {
        docID = NO_MORE_DOCS;
      }
      if (docID < size) {
        value = iter.next();
      } else {
        value = MISSING;
      }
      return docID;
    }

