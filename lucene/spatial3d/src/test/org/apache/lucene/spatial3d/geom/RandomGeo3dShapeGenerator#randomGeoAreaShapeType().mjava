  /**
   * Method that returns a random generated GeoAreaShape code from all supported GeoAreaShapes.
   *
   * <p>We are removing Collections because it is difficult to create shapes with properties in some
   * cases.
   *
   * @return a random generated polygon code
   */
  public int randomGeoAreaShapeType() {
    return random().nextInt(12);
  }

