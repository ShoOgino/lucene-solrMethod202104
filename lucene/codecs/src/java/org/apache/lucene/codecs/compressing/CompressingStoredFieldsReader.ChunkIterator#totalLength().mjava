    // total length of the chunk
    private int totalLength() {
      int totalLength = 0;
      for (int i = 0; i < chunkDocs; ++i) {
        totalLength += lengths[i];
      }
      return totalLength;
    }

