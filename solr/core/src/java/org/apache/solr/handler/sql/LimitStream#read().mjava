  public Tuple read() throws IOException {
    ++count;
    if(count > limit) {
      Map<String, String> fields = new HashMap<>();
      fields.put("EOF", "true");
      return new Tuple(fields);
    }

    return stream.read();
  }

