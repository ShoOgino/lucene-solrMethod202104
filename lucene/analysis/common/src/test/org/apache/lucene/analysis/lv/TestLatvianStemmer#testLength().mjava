  /** Test some length restrictions, we require a 3+ char stem, with at least one vowel. */
  public void testLength() throws IOException {
    checkOneTerm(a, "usa", "usa"); // length
    checkOneTerm(a, "60ms", "60ms"); // vowel count
  }

