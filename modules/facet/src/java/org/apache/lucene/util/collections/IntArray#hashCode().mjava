  /**
   * Calculates a hash-code for HashTables
   */
  @Override
  public int hashCode() {
    int hash = 0;
    for (int i = 0; i < size; ++i) {
      hash = data[i] ^ (hash * 31);
    }
    return hash;
  }

