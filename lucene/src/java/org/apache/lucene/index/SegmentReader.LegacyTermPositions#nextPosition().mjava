    public int nextPosition() throws IOException {
      pendingPayload = null;
      if (!any || postingsEnum == null) {
        return 0;
      } else {
        return postingsEnum.nextPosition();
      }
    }

