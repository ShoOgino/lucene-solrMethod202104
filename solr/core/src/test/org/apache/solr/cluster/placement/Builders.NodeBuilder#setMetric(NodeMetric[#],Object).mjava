    public NodeBuilder setMetric(NodeMetric<?> metric, Object value) {
      if (metrics == null) {
        metrics = new HashMap<>();
      }
      metrics.put(metric, metric.convert(value));
      return this;
    }

