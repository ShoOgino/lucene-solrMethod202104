  private Set<String> setOf(String... keys) {
    Set<String> set = new TreeSet<String>();
    if (keys != null) {
      for (String k : keys) {
        set.add(k);
      }
    }
    return set;
  }

