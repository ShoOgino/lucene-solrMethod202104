  @Override
  protected void destroyServers() throws Exception {
    for (JettySolrRunner runner : jettys) {
      try {
        runner.stop();
      } catch (Exception e) {
        log.error("", e);
      }
    }

    jettys.clear();
  }

