  /**
   * Perform the task once (ignoring repetitions specification) Return number of work items done by
   * this task. For indexing that can be number of docs added. For warming that can be number of
   * scanned items, etc.
   *
   * @return number of work items done by this task.
   */
  public abstract int doLogic() throws Exception;

