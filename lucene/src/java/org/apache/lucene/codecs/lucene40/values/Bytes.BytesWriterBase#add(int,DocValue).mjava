    @Override
    public void add(int docID, DocValue docValue) throws IOException {
      final BytesRef ref;
      if ((ref = docValue.getBytes()) != null) {
        add(docID, ref);
      }
    }

