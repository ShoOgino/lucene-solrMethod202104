  @Override
  protected boolean lessThan(OrdAndCount a, OrdAndCount b) {
    if (a.count < b.count) {
      return true;
    } else if (a.count > b.count) {
      return false;
    } else {
      return a.ord > b.ord;
    }
  }

