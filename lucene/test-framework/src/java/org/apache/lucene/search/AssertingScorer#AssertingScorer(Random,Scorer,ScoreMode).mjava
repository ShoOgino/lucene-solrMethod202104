  private AssertingScorer(Random random, Scorer in, ScoreMode scoreMode) {
    super(in.weight);
    this.random = random;
    this.in = in;
    this.scoreMode = scoreMode;
    this.doc = in.docID();
  }

