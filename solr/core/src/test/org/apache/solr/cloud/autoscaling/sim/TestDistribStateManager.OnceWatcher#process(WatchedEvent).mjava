    @Override
    public void process(WatchedEvent event) {
      if (triggered.getCount() == 0) {
        fail("Watch was already triggered once!");
      }
      triggered.countDown();
      this.event = event;
    }

