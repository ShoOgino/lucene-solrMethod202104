    @Override
    public float maxScore() {
      switch(scoreMode) {
        case Max:
        case Min:
          return childScorer.maxScore();
        default:
          return Float.POSITIVE_INFINITY;
      }
    }

