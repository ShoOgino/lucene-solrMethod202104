    public static MultiCoreAction get( String p )
    {
      if( p != null ) {
        try {
          return MultiCoreAction.valueOf( p.toUpperCase() );
        }
        catch( Exception ex ) {}
      }
      return null; 
    }

