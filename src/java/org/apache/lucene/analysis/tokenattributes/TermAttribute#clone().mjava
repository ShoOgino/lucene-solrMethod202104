  public Object clone() {
    TermAttribute t = (TermAttribute)super.clone();
    // Do a deep clone
    if (termBuffer != null) {
      t.termBuffer = (char[]) termBuffer.clone();
    }
    return t;
  }

