    @Override
    public void read(IndexInput indexIn, boolean absolute)
      throws IOException {
      if (absolute) {
        fp = indexIn.readVLong();
        first = false;
      } else {
        assert !first;
        fp += indexIn.readVLong();
      }
    }

