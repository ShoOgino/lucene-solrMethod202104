  /** Close all indexes */
  protected void closeAll() throws Exception {
    // close and nullify everything
    taxoReader.close();
    taxoReader = null;
    indexReader.close();
    indexReader = null;
    searcher.close();
    searcher = null;
    indexDir = null;
    taxoDir = null;
  }

