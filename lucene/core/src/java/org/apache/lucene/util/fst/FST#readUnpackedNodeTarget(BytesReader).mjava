  private int readUnpackedNodeTarget(BytesReader in) throws IOException {
    int target;
    if (version < VERSION_VINT_TARGET) {
      target = in.readInt();
    } else {
      target = in.readVInt();
    }
    return target;
  }

