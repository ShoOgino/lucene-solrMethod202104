  /**
   * Returns the Query causing the current match
   *
   * <p>If this {@link MatchesIterator} has been returned from a {@link #getSubMatches()} call, then
   * returns a {@link TermQuery} equivalent to the current match
   *
   * <p>Should only be called after {@link #next()} has returned {@code true}
   */
  Query getQuery();

