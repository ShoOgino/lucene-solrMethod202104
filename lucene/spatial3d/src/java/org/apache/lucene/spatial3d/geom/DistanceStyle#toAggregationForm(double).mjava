  /**
   * Convert a distance to a form meant for aggregation. This is meant to be used in conjunction
   * with aggregateDistances() and fromAggregationForm(). Distances should be converted to
   * aggregation form before aggregation is attempted, and they should be converted back from
   * aggregation form to yield a final result.
   *
   * @param distance is an output of computeDistance().
   * @return the distance, converted to aggregation form.
   */
  public default double toAggregationForm(final double distance) {
    return distance;
  }

