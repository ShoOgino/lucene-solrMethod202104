  /**
   * Get {@link Policy} configuration.
   */
  public Policy getPolicy() {
    if (policy == null) {
      if (jsonMap != null) {
        policy = new Policy(jsonMap);
      } else {
        policy = new Policy();
      }
    }
    return policy;
  }

