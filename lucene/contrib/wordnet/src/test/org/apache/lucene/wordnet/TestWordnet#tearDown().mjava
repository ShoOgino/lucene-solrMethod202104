  @Override
  public void tearDown() throws Exception {
    if (searcher != null) {
      searcher.close();
    }
    if (dir != null) {
      dir.close();
    }
    rmDir(storePathName); // delete our temporary synonym index
    super.tearDown();
  }

