  @Override
  public void tearDown() throws Exception {
    if (null != reader) {
      reader.close();
      reader = null;
    }
    if (null != directory) {
      directory.close();
      directory = null;
    }
    if (null != analyzer) {
      analyzer.close();
      analyzer = null;
    }
    super.tearDown();
  }

