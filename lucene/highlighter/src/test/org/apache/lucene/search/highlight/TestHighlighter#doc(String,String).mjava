  private Document doc(String name, String value) {
    Document d = new Document();
    d.add(
        new Field(
            name, value, fieldType)); // fieldType is randomly chosen for term vectors in setUp
    return d;
  }

