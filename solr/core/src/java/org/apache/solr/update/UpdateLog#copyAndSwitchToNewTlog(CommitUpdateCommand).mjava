  protected void copyAndSwitchToNewTlog(CommitUpdateCommand cuc) {
    synchronized (this) {
      if (tlog == null) return;
      preCommit(cuc);
      try {
        copyOverOldUpdates(cuc.getVersion());
      } finally {
        postCommit(cuc);
      }
    }
  }

