    /** Encodes as 1 or 2 ints, and can only use 61 of the 64
     *  long bits. */
    public long readVLong() throws IOException {
      final int v = next();
      if ((v & 1) == 0) {
        return v >> 1;
      } else {
        final long v2 = next();
        return (v2 << 30) | (v >> 1);
      }
    }

