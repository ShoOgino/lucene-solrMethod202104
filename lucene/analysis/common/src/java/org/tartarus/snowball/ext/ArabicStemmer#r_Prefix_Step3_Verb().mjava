private boolean r_Prefix_Step3_Verb() {
  int among_var;
  bra = cursor;
  among_var = find_among(a_8);
  if (among_var == 0)
  {
    return false;
  }
  ket = cursor;
  switch (among_var) {
    case 1:
      if (!(limit > 4))
      {
        return false;
      }
      slice_from("\u064A");
      break;
    case 2:
      if (!(limit > 4))
      {
        return false;
      }
      slice_from("\u062A");
      break;
    case 3:
      if (!(limit > 4))
      {
        return false;
      }
      slice_from("\u0646");
      break;
    case 4:
      if (!(limit > 4))
      {
        return false;
      }
      slice_from("\u0623");
      break;
  }
  return true;
}

