  private synchronized void refreshReader() throws IOException {
    if (reader != null) {
      IndexReader r2 = reader.reopen();
      if (reader != r2) {
        reader.close();
        reader = r2;
      }
    }
  }

