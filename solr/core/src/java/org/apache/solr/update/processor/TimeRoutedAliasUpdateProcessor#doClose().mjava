  @Override
  protected void doClose() {
    try {
      cmdDistrib.close();
    } finally {
      try {
        super.doClose();
      } finally {
        ExecutorUtil.shutdownAndAwaitTermination(preemptiveCreationWaitExecutor);
      }
    }
  }

