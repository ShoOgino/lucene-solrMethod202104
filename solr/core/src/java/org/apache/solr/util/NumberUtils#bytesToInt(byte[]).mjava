  public static int bytesToInt(byte[] bytes) {
    if (bytes == null) return 0;
    int val = 0;
    for (int i = 0; i < bytes.length; i++) {
      val = val << 8;
      val += bytes[i];
    }
    return val;
  }

