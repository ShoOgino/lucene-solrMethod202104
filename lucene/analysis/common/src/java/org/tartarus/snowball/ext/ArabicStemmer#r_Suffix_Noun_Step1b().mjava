private boolean r_Suffix_Noun_Step1b() {
  ket = cursor;
  if (find_among_b(a_11) == 0)
  {
    return false;
  }
  bra = cursor;
  if (!(limit > 5))
  {
    return false;
  }
  slice_del();
  return true;
}

