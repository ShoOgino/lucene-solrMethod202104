      public InstrumentedIndexReader(IndexReader in) {
        super(in);
        this.in = in;
        mynum = ireader++;
        openReaders.add(mynum);
        //        System.err.println("opened "+mynum);
      }

