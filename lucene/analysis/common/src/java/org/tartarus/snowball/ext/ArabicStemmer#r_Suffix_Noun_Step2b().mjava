private boolean r_Suffix_Noun_Step2b() {
  ket = cursor;
  if (find_among_b(a_13) == 0)
  {
    return false;
  }
  bra = cursor;
  if (!(limit >= 5))
  {
    return false;
  }
  slice_del();
  return true;
}

