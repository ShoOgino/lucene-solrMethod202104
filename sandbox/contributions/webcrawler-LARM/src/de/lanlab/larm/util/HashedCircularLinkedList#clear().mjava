    /**
     * Removes all of the elements from this list.
     */
    public void clear()
    {
        // list
        header.next = header.previous = header;

        // hash
        keys.clear();

        size = 0;
        current = null;
    }

