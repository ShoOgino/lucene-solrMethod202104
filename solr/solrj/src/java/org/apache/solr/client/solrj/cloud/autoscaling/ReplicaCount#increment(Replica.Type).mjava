  public void increment(Replica.Type type) {
    switch (type) {
      case NRT:
        nrt++;
        break;
      case PULL:
        pull++;
        break;
      case TLOG:
        tlog++;
        break;
      default:
        nrt++;
    }
  }

