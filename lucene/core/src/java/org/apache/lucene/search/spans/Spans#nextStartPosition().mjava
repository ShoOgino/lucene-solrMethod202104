  /**
   * Returns the next start position for the current doc. There is always at least one start/end
   * position per doc. After the last start/end position at the current doc this returns {@link
   * #NO_MORE_POSITIONS}.
   */
  public abstract int nextStartPosition() throws IOException;

