    public SolrRequest getOperation() {
      if (!isInitialized) {
        session.applyRules();
        originalViolations.addAll(session.getViolations());
        this.operation = init();
        isInitialized = true;
      }
      return operation;
    }

