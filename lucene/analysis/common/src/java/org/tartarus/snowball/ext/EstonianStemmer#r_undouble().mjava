    private boolean r_undouble() {
        // (, line 144
        // next, line 145
        if (cursor <= limit_backward)
        {
            return false;
        }
        cursor--;
        // [, line 145
        ket = cursor;
        // hop, line 145
        {
            int c = cursor - 1;
            if (limit_backward > c || c > limit)
            {
                return false;
            }
            cursor = c;
        }
        // ], line 145
        bra = cursor;
        // delete, line 145
        slice_del();
        return true;
    }

