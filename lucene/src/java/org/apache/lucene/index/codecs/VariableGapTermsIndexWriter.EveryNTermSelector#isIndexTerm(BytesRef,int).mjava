    @Override
    public boolean isIndexTerm(BytesRef term, int docFreq) {
      if (count >= interval) {
        count = 0;
        return true;
      } else {
        count++;
        return false;
      }
    }

