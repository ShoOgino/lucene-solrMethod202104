  /**
   * Calendar utility method: Returns a clone of the {@link Calendar} passed to the constructor with
   * all fields cleared.
   */
  public Calendar newCal() {
    return (Calendar) CAL_TMP.clone();
  }

