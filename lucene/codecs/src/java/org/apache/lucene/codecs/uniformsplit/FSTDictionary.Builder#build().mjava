    @Override
    public FSTDictionary build() {
      try {
        return new FSTDictionary(fstCompiler.compile());
      } catch (IOException e) {
        // Should never happen.
        throw new RuntimeException(e);
      }
    }

