  @Override
  public void close() throws IOException {
    if (zkStateReader != null && closeZkStateReader) {
      synchronized (this) {
        if (zkStateReader != null)
          zkStateReader.close();
        zkStateReader = null;
      }
    }
  }

