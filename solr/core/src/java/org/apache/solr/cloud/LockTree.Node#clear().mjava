    void clear() {
      if (myLock != null) {
        log.warn("lock_is_leaked at {}", constructPath(new LinkedList<>()));
        myLock = null;
      }
      for (Node node : children.values()) node.clear();
    }

