  @Override
  public void reset(Reader input) throws IOException {
    super.reset(input);
    resultToken.setLength(0);
    finalOffset = 0;
    skipped = 0;
    delimitersCount = -1;
    delimiterPositions.clear();
  }

