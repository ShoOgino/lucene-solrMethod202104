    @Override
    public Number next() {
      if (!hasNext()) {
        throw new NoSuchElementException();
      }
      int ord = (int) iter.next();
      ordUpto++;
      // TODO: make reusable Number
      return ordMap[ord];
    }

