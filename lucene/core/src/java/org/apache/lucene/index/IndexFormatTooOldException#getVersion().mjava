  /**
   * Returns the version of the file that was too old. This method will return <code>null</code> if
   * an alternative {@link #getReason()} is provided.
   */
  public Integer getVersion() {
    return version;
  }

