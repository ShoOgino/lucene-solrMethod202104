    /**
     * Add a new {@link Term} to this builder, with a default boost of {@code 1}.
     *
     * @see #add(Term, float)
     */
    public Builder add(Term term) {
      return add(term, 1f);
    }

