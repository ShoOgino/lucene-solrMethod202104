    void close(boolean doWait) throws Throwable {
      didClose = true;
      if (doWait == false) {
        writer2.abortMerges();
      }
      //writer2.shutdown();
      writer2.rollback();
    }

