    /**
     * Closes the ReuseStrategy, freeing any resources
     */
    @Override
    public void close() {
      if (storedValue != null) {
        storedValue.close();
        storedValue = null;
      }
    }

