    private boolean r_Prefix_Step3_Verb() {
        int among_var;
        // (, line 401
        I_word_len = getCurrent().length();
        // [, line 403
        bra = cursor;
        // substring, line 403
        among_var = find_among(a_8, a_8.length);
        if (among_var == 0)
        {
            return false;
        }
        // ], line 403
        ket = cursor;
        switch (among_var) {
            case 0:
                return false;
            case 1:
                // (, line 405
                if (!(I_word_len > 4))
                {
                    return false;
                }
                // <-, line 405
                slice_from("\u064A");
                break;
            case 2:
                // (, line 406
                if (!(I_word_len > 4))
                {
                    return false;
                }
                // <-, line 406
                slice_from("\u062A");
                break;
            case 3:
                // (, line 407
                if (!(I_word_len > 4))
                {
                    return false;
                }
                // <-, line 407
                slice_from("\u0646");
                break;
            case 4:
                // (, line 408
                if (!(I_word_len > 4))
                {
                    return false;
                }
                // <-, line 408
                slice_from("\u0623");
                break;
        }
        return true;
    }

