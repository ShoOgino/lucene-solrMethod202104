    public void close() throws IOException {
      assert !isClosed.getAndSet(true);
      ex =new Exception();
      if (datIn != null) {
        datIn.close();
      }
      if (idxIn != null) {
        idxIn.close();
      }
    }

