    /**
     *   join messageHandler-Thread
     */
    public void finalize()
    {
        if (t != null)
        {
            try
            {
                t.join();
                t = null;
            }
            catch(InterruptedException e) {}
        }
    }

