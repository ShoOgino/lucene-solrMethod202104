    /**
     *  The teardown method for JUnit
     * TODO: remove indexDir?
     */
    @Override
    public void tearDown() throws IOException {
        searcher.close();
        dir.close();
    }

