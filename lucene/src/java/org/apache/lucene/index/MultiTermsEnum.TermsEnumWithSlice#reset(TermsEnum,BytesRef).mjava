    public void reset(TermsEnum terms, BytesRef term) {
      this.terms = terms;
      current = term;
      // TODO: can we not null these?
      reuseDocs = null;
      reusePostings = null;
    }

