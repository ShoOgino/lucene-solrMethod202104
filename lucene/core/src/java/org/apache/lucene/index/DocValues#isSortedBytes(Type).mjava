  // nocommit nuke
  public static boolean isSortedBytes(Type type) {
    switch(type) {
    case BYTES_VAR_SORTED:
    case BYTES_FIXED_SORTED:
      return true;
    default:
      return false;
    }
  }

