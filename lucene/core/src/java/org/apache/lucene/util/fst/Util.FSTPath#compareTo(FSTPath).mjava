    @Override
    public int compareTo(FSTPath other) {
      if (cost < other.cost) {
        return -1;
      } else if (cost > other.cost) {
        return 1;
      } else  {
        return input.compareTo(other.input);
      }
    }

