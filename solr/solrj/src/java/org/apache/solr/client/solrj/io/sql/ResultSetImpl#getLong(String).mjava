  @Override
  public long getLong(String columnLabel) throws SQLException {
    Long l =  tuple.getLong(columnLabel);
    if(l == null) {
      return 0;
    } else {
      return l.longValue();
    }
  }

