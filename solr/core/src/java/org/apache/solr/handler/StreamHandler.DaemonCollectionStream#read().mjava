    @SuppressWarnings({"unchecked"})
    public Tuple read() {
      if (it.hasNext()) {
        return it.next().getInfo();
      } else {
        @SuppressWarnings({"rawtypes"})
        Map m = new HashMap();
        m.put("EOF", true);
        return new Tuple(m);
      }
    }

