    public int read() throws IOException
    {
        int readByte = super.read();
        if(reporting)
        {
            notifyObserver(readByte>=0? 1 : 0);
        }
        return readByte;
    }

