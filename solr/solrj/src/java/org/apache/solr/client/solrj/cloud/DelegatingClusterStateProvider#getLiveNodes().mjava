    @Override
    public Set<String> getLiveNodes() {
        if (delegate != null) {
            return delegate.getLiveNodes();
        } else {
            return Collections.emptySet();
        }
    }

