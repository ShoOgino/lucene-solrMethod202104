    /** sugar: returns ordinal with maxFreq() */
    public int maxOrd() {
      long max = 0;
      int maxOrd = 0;
      for (int i = 0; i < size; i++) {
        if (freqs[i] > max) {
          max = freqs[i];
          maxOrd = i;
        }
      }
      return maxOrd;
    }

