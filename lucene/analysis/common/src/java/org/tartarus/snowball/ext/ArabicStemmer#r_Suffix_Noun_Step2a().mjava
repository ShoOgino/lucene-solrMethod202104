private boolean r_Suffix_Noun_Step2a() {
  ket = cursor;
  if (find_among_b(a_12) == 0)
  {
    return false;
  }
  bra = cursor;
  if (!(limit > 4))
  {
    return false;
  }
  slice_del();
  return true;
}

