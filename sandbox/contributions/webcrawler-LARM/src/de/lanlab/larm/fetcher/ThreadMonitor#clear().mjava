    public synchronized void clear()
    {
        //sampleTimeStamps.clear();
        /*for(int i=0; i < timeSamples.length; i++)
        {
            timeSamples[i].clear();
        }
        */
    }

