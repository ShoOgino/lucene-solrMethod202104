  /** Creates a new {@link CharsRef} initialized with the given Strings character array */
  public CharsRef(String string) {
    this.chars = string.toCharArray();
    this.offset = 0;
    this.length = chars.length;
  }

