    void close(boolean doWait) throws Throwable {
      didClose = true;
      if (doWait) {
        mainWriter.waitForMerges();
      } else {
        mainWriter.abortMerges();
      }
      mainWriter.close();
    }

