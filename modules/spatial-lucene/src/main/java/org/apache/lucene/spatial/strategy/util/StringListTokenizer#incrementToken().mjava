  @Override
  public boolean incrementToken() {
    if (iter.hasNext()) {
      clearAttributes();
      String t = iter.next();
      termAtt.append(t);
      return true;
    }
    return false;
  }

