  @Override
  public void close() {
    log.debug("Closing " + hashCode());
    if (collectService != null) {
      collectService.shutdownNow();
    }
    if (factory != null) {
      factory.close();
    }
    knownDbs.clear();
  }

