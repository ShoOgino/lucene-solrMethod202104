  @Before
  public void closePreviousCluster() throws Exception {
    shutdownCluster();
    leaderRegistries = 0;
    clusterRegistries = 0;
    jmxReporter = JmxUtil.findFirstMBeanServer() != null ? 1 : 0;
  }

