    @SuppressWarnings({"unchecked"})
    public Tuple read() {
      String msg = e.getMessage();
      @SuppressWarnings({"rawtypes"})
      Map m = new HashMap();
      m.put("EOF", true);
      m.put("EXCEPTION", msg);
      return new Tuple(m);
    }

