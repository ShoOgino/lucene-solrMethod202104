  /** Creates a new {@link CharsRef} initialized with the given array, offset and length */
  public CharsRef(char[] chars, int offset, int length) {
    this.chars = chars;
    this.offset = offset;
    this.length = length;
    assert isValid();
  }

