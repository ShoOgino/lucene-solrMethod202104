    @Override
    public void consumeTerms(Query query, Term... terms) {
      if (defSize > 0) {
        total += defSize;
      } else {
        total += shallowSizeOf(query);
      }
      if (terms != null) {
        for (Term t : terms) {
          total += sizeOf(t);
        }
      }
    }

