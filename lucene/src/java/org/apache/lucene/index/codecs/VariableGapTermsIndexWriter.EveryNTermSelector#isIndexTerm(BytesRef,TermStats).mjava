    @Override
    public boolean isIndexTerm(BytesRef term, TermStats stats) {
      if (count >= interval) {
        count = 0;
        return true;
      } else {
        count++;
        return false;
      }
    }

