  @Override
  public Object clone() {
    CharTermAttributeImpl t = (CharTermAttributeImpl)super.clone();
    // Do a deep clone
    if (termBuffer != null) {
      t.termBuffer = termBuffer.clone();
    }
    return t;
  }

