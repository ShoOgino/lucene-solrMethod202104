  @Override
  public Iterator<Map<String,Object>> iterator(Object key) {
    checkOpen(true);
    List<Map<String,Object>> val = theMap.get(key);
    if (val == null) {
      return null;
    }
    return val.iterator();
  }

