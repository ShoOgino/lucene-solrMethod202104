  /**
   * @return true if at least one field has any vectors
   */
  public boolean hasVectors() {
    for (FieldInfo fi : this) {
      if (fi.hasVectors()) {
        return true;
      }
    }
    return false;
  }

