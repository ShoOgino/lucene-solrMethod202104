    @Override
    public long longValue() {
      assertThread("Numeric doc values", creationThread);
      assert in.docID() != -1;
      assert in.docID() != NO_MORE_DOCS;
      return in.longValue();
    }    

