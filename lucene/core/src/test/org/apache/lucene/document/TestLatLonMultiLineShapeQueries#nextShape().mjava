  @Override
  protected Line[] nextShape() {
    int n = random().nextInt(4) + 1;
    Line[] lines = new Line[n];
    for (int i = 0; i < n; i++) {
      lines[i] = nextLine();
    }
    return lines;
  }

