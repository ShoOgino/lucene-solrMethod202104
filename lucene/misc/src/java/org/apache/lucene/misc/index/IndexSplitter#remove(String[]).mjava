  public void remove(String[] segs) throws IOException {
    for (String n : segs) {
      SegmentCommitInfo info = getInfo(n);
      infos.remove(info);
    }
    infos.changed();
    infos.commit(fsDir);
  }

