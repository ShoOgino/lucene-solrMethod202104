    @Override
    public long nextOrd() {
      assert lastOrd != NO_MORE_ORDS;
      long ord = in.nextOrd();
      assert ord == NO_MORE_ORDS || ord < valueCount;
      assert ord > lastOrd;
      lastOrd = ord;
      return ord;
    }

