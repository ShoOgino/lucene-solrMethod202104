            public long skip(long n) throws IOException
            {
                n = Math.min(n, size-offset);
                n = pageFileIS.skip(n);
                if(n > 0)
                {
                    offset+=n;
                }
                return n;
            }

