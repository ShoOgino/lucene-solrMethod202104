    public int compareBottom(int doc, float score) {
      if (bottom > score) {
        return -1;
      } else if (bottom < score) {
        return 1;
      } else {
        return 0;
      }
    }

