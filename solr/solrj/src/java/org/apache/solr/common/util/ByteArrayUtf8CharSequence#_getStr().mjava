  private String _getStr() {
    if (utf16 == null) {
      synchronized (this) {
        if (utf16 == null) {
          CharArr arr = new CharArr();
          ByteUtils.UTF8toUTF16(buf, offset, length, arr);
          utf16 = arr.toString();
        }
      }
    }
    return utf16;
  }

