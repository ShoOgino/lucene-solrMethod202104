    @Override
    public FSTDictionary build() {
      try {
        return new FSTDictionary(fstBuilder.finish());
      } catch (IOException e) {
        // Should never happen.
        throw new RuntimeException(e);
      }
    }

