    @Override
    public void close() throws IOException {
      try {
        super.close();
      } catch (Throwable t) {
        LOG.error("Error while closing", t);
      }
      writer.close();
    }

