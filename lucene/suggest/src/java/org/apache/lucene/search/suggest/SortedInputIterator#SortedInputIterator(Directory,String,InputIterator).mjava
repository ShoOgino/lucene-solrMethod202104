  /**
   * Creates a new sorted wrapper, using {@linkplain Comparator#naturalOrder() natural order} for
   * sorting.
   */
  public SortedInputIterator(Directory tempDir, String tempFileNamePrefix, InputIterator source)
      throws IOException {
    this(tempDir, tempFileNamePrefix, source, Comparator.naturalOrder());
  }

