  @Override
  public void tearDown() throws Exception {
    super.tearDown();
    if (cluster != null) {
      if (log.isInfoEnabled()) {
        log.info(cluster.dumpClusterState(false));
      }
    }
  }

