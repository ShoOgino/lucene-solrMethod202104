    /** Sole constructor */
    public FSTPath(T cost, FST.Arc<T> arc, IntsRefBuilder input) {
      this.arc = new FST.Arc<T>().copyFrom(arc);
      this.cost = cost;
      this.input = input;
    }

