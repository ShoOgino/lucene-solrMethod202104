private boolean r_Prefix_Step1() {
  int among_var;
  bra = cursor;
  among_var = find_among(a_4);
  if (among_var == 0)
  {
    return false;
  }
  ket = cursor;
  switch (among_var) {
    case 1:
      if (!(limit > 3))
      {
        return false;
      }
      slice_from("\u0623");
      break;
    case 2:
      if (!(limit > 3))
      {
        return false;
      }
      slice_from("\u0622");
      break;
    case 3:
      if (!(limit > 3))
      {
        return false;
      }
      slice_from("\u0627");
      break;
    case 4:
      if (!(limit > 3))
      {
        return false;
      }
      slice_from("\u0625");
      break;
  }
  return true;
}

