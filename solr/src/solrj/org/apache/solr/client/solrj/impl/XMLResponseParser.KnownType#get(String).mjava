    public static KnownType get( String v )
    {
      if( v != null ) {
        try {
          return KnownType.valueOf( v.toUpperCase() );
        }
        catch( Exception ex ) {}
      }
      return null;
    }

