    @Override
    public Comparator<BytesRef> comparator() {
      try {
        return termsEnum.getComparator();
      } catch (IOException e) {
        throw new RuntimeException(e);
      }
    }

