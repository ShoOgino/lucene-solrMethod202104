  private AssertingScorer(Random random, Scorer in, boolean needsScores) {
    super(in.weight);
    this.random = random;
    this.in = in;
    this.needsScores = needsScores;
    this.doc = in.docID();
  }

