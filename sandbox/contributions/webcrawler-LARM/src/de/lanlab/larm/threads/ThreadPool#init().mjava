    /**
     *  this init method will create the tasks. It must be called by hand
     */
    public void init() {
        for (int i = 0; i < maxThreads; i++) {
            createThread(i);
        }
    }

