  /**
   * Remove all fields and boosts from the document
   */
  @Override
  public void clear()
  {
    if( _fields != null ) {
      _fields.clear();      
    }
    if (_childDocuments != null) {
      _childDocuments.clear();
    }
  }

