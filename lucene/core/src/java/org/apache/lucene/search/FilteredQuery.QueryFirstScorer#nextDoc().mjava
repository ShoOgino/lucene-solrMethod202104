    @Override
    public int nextDoc() throws IOException {
      int doc;
      for(;;) {
        doc = scorer.nextDoc();
        if (doc == DocIdSetIterator.NO_MORE_DOCS || filterBits.get(doc)) {
          return doc;
        }
      } 
    }

