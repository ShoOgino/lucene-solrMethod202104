public boolean r_V() {
  int v_1 = limit - cursor;
  {
    int c = cursor - 2;
    if (limit_backward > c || c > limit)
    {
      return false;
    }
    cursor = c;
  }
  cursor = limit - v_1;
  if (!(eq_s_b("c")))
  {
    return false;
  }
  return true;
}

