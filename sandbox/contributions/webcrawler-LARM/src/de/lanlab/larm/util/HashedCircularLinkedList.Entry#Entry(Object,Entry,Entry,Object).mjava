        Entry(Object element, Entry next, Entry previous, Object key)
        {
            this.element = element;
            this.next = next;
            this.previous = previous;
            this.key = key;
        }

