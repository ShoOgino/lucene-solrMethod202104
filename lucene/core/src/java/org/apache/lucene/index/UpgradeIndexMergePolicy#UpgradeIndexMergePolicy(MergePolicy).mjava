  /**
   * Wrap the given {@link MergePolicy} and intercept forceMerge requests to only upgrade segments
   * written with previous Lucene versions.
   */
  public UpgradeIndexMergePolicy(MergePolicy in) {
    super(in);
  }

