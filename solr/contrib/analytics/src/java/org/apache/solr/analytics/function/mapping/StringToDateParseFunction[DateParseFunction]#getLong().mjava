  @Override
  public long getLong() {
    long value = 0;
    try {
      value = formatter.parse(param.toString()).getTime();
      exists = param.exists();
    } catch (ParseException e) {
      exists = false;
    }
    return value;
  }

