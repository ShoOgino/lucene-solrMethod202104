    @Override
    public int hashCode() {
      int result = collection.hashCode();
      result = 31 * result + shard.hashCode();
      result = 31 * result + leader.hashCode();
      return result;
    }

